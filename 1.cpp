#include<bits/stdc++.h>
#define ll long long int
#define vvi vector<vector<ll> >
#define rep(i,a,b) for (int i = a; i < b; ++i)

using namespace std;



int main() {
	int t; cin>>t;
	while(t--) {
		int n, q; cin >> n >> q;
		vvi adj(n + 1, vectot<int>(n + 1));
		
		if(L(s) == V(s)) {
			cout << "YES" << endl;
		}else {
			cout << "NO" << endl;
		}
	}
	return 0;

}
